{
  "installCommand": "cd ../.. && NODE_ENV=development pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @repo/types build && pnpm --filter backend build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
